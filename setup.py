from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a nlp project to analyse the youtube comment sentiments',
    author='sarvesh',
    license='',
)
